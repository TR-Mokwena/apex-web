# Stream Video (getstream.io) — Conference Room
# Get these from https://dashboard.getstream.io → your app → "Video & Audio"
# The API key is public (shipped to the browser). The secret is server-only.
NEXT_PUBLIC_STREAM_API_KEY=your_stream_api_key
STREAM_API_SECRET=your_stream_api_secret
